# ── Vite Dev Server ───────────────────────────
# The backend URL — keep as-is for local dev
VITE_API_URL=http://localhost:5000

# ── Google OAuth (same Client ID as backend) ──
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
